--- LicenseHeaderReplacer.cs
+++ LicenseHeaderReplacer.cs
@@ -242,7 +244,15 @@
 
     private bool IsLink (ProjectItem item)
     {
-      return (item.Properties != null && (bool) item.Properties.Item("IsLink").Value);
+      if (item.Properties == null) return false;
+
+      Property isLinkProperty = item.Properties.Item("IsLink");
+      if (isLinkProperty != null)
+      {
+        return (bool)isLinkProperty.Value;
+      }
+
+      return false;
     }
   }
 }
